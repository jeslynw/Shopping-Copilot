pytest>=8          # tests/
jsonschema>=4      # cross-checks responses against docs/agent_api_contract.json (the agent ships its own pure-python check)
anthropic>=0.40    # OPTIONAL: Claude message polisher for the demo only, opt-in via COPILOT_LLM=1 — never in the scored path
