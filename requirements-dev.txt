pytest>=8          # tests/
jsonschema>=4      # cross-checks responses against docs/agent_api_contract.json (the agent ships its own pure-python check)
anthropic>=1.2     # Claude layer (grounded extraction fallback + message polish); on iff ANTHROPIC_API_KEY is set
